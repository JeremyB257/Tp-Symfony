{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	- Accueil
{% endblock %}

{% block body %}
	<main class="container-lg mt-5 text-center">
		<form action="{{path('event.index')}}" class="d-flex mb-5" role="search">
			<input class="form-control me-2" type="search" name="search" placeholder="Search" aria-label="Recherche ...">
			<button class="btn btn-primary" type="submit">Rechercher</button>
		</form>
		<h1 class="mt-5">Bienvenue sur Event Time</h1>
		<a href="{{path('event.index')}}" class="btn btn-primary mt-5">
			Voir la liste des événements
		</a>
	</main>
{% endblock %}
